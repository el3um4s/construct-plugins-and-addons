"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.StraniAnelli_Leaflet;

	PLUGIN_CLASS.Type = class LeafletType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
