"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.stranianelli_random_color;
	
	PLUGIN_CLASS.Type = class stranianelli_random_colorType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}