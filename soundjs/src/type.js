"use strict";

{
    const BEHAVIOR_CLASS = SDK.Behaviors.stranianelliSoundJS;

    BEHAVIOR_CLASS.Type = class stranianelliSoundJSType extends SDK.IBehaviorTypeBase {
        constructor(sdkPlugin, iBehaviorType) {
            super(sdkPlugin, iBehaviorType);
        }
    };
}
